import { Component, h } from '@stencil/core';

@Component({
    tag: 'tab-random',
    styleUrl: 'tab-random.css',
    // shadow: true,
})
export class TabRandom {
    render() {
        return 'Random';
    }
}
